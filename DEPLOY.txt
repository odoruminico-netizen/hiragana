Cloudflare Workers Build settings

Build command:
rm -rf dist && mkdir dist && cp index.html app.js icon-180.png dist/

Deploy command:
npx wrangler deploy --assets ./dist --compatibility-date 2026-08-11

Root directory:
/

Note: icon-512.png is included in the archive for future PWA use but is not required by the current build command.
