Hiraganada ver0.2 Cloudflare Workers Static Assets

GitHub root files:
- index.html
- app.js
- icon-180.png

Build command:
rm -rf dist && mkdir dist && cp index.html app.js icon-180.png dist/

Deploy command:
npx wrangler deploy --assets ./dist --compatibility-date 2026-08-11

Expected log: Read 3 files from the assets directory .../dist
