Cloudflare build settings (existing settings can stay):

Build command:
rm -rf dist && mkdir dist && cp index.html app.js icon-180.png dist/

Deploy command:
npx wrangler deploy --assets ./dist --compatibility-date 2026-08-11

The app no longer depends on app.js at runtime; app.js is kept only so the existing build command continues to work.
